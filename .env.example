# Enter real values ONLY in a local .env.local file, never in browser code.
REALITY_DEFENDER_API_KEY=
GROQ_API_KEY=
# Fixture mode never contacts providers. Live mode never falls back to fixtures.
REALCHECK_MODE=fixture
PORT=3000

